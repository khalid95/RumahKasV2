@extends('admin.layout',['title'=>'Lisensi']) @section('content')<section class="overflow-x-auto rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[.03]"><table class="w-full text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-5 py-3">Pemilik</th><th>Produk</th><th>Lisensi</th><th>Perangkat</th><th>Kelola</th></tr></thead><tbody>@forelse($licenses as $license)<tr class="border-t border-gray-100"><td class="px-5 py-4"><b>{{ $license->user->name }}</b><div class="text-gray-500">{{ $license->user->email }}</div></td><td>{{ $license->product->name }}</td><td><span class="{{ $license->status==='active'?'text-success-600':'text-error-600' }}">{{ $license->status }}</span><div class="text-xs text-gray-400">••••{{ $license->key_last_four }}</div></td><td>{{ $license->installations->whereNull('revoked_at')->count() }}/{{ $license->max_installations }}</td><td class="pr-5"><form method="POST" action="{{ route('admin.licenses.update',$license) }}" class="flex gap-2">@csrf @method('PUT')<input name="max_installations" type="number" min="1" max="10" value="{{ $license->max_installations }}" class="w-16 rounded-lg border px-2"><select name="status" class="rounded-lg border px-2"><option value="active" @selected($license->status==='active')>active</option><option value="revoked" @selected($license->status==='revoked')>revoked</option></select><button class="text-brand-600">Simpan</button></form></td></tr>@empty<tr><td colspan="5" class="p-12 text-center">Belum ada lisensi.</td></tr>@endforelse</tbody></table></section><div class="mt-4">{{ $licenses->links() }}</div>@endsection
