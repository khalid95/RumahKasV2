@extends('admin.layout', ['title' => $title])
@section('content')
<section class="overflow-hidden rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[0.03]"><header class="border-b border-gray-100 px-5 py-4 dark:border-gray-800"><h2 class="font-semibold">{{ $title }}</h2></header><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500 dark:bg-white/[0.03]"><tr>@foreach($columns as $column)<th class="whitespace-nowrap px-5 py-3">{{ $column }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr class="border-t border-gray-100 dark:border-gray-800">@foreach($row as $value)<td class="whitespace-nowrap px-5 py-4 text-gray-700 dark:text-gray-300">{{ $value }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($columns) }}" class="px-5 py-12 text-center text-gray-500">Belum ada data.</td></tr>@endforelse</tbody></table></div>@if($rows->hasPages())<div class="border-t border-gray-100 px-5 py-4 dark:border-gray-800">{{ $rows->links() }}</div>@endif</section>
@endsection
