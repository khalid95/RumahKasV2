@extends('admin.layout',['title'=>'Pembayaran']) @section('content')<div x-data="{open:false}" class="space-y-5"><div class="flex justify-end"><button @click="open=true" class="rounded-lg bg-brand-500 px-4 py-2.5 text-sm font-semibold text-white">+ Catat Pembayaran</button></div><section class="overflow-x-auto rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[.03]"><table class="w-full text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-5 py-3">Pengguna</th><th>Referensi</th><th>Nominal</th><th>Status</th><th></th></tr></thead><tbody>@forelse($payments as $payment)<tr class="border-t border-gray-100"><td class="px-5 py-4">{{ $payment->user->email }}</td><td>{{ $payment->provider_reference ?: '-' }}</td><td>Rp{{ number_format($payment->amount,0,',','.') }}</td><td>{{ $payment->status }} @if($payment->license)<span class="text-success-600">• lisensi</span>@endif</td><td class="pr-5"><form method="POST" action="{{ route('admin.payments.update',$payment) }}">@csrf @method('PUT')<select name="status" onchange="this.form.submit()" class="rounded-lg border border-gray-300 bg-transparent px-2 py-1"><option selected disabled>Ubah</option>@foreach(['pending','paid','failed','refunded'] as $s)<option>{{ $s }}</option>@endforeach</select></form></td></tr>@empty<tr><td colspan="5" class="p-12 text-center">Belum ada pembayaran.</td></tr>@endforelse</tbody></table></section>{{ $payments->links() }}<div x-show="open" class="fixed inset-0 z-[99999] grid place-items-center bg-gray-950/50 p-4" @click.self="open=false"><form method="POST" action="{{ route('admin.payments.store') }}" class="w-full max-w-lg space-y-4 rounded-2xl bg-white p-6 dark:bg-gray-900">@csrf<h2 class="text-xl font-bold">Catat pembayaran</h2><select name="user_id" required class="h-11 w-full rounded-lg border px-3"><option value="">Pilih pengguna</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }} — {{ $user->email }}</option>@endforeach</select><select name="product_id" required class="h-11 w-full rounded-lg border px-3">@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select><input name="provider" value="manual" required class="h-11 w-full rounded-lg border px-3" placeholder="Provider"><input name="provider_reference" class="h-11 w-full rounded-lg border px-3" placeholder="Referensi"><input name="amount" type="number" min="0" required class="h-11 w-full rounded-lg border px-3" placeholder="Nominal"><select name="status" class="h-11 w-full rounded-lg border px-3"><option value="paid">Lunas — terbitkan lisensi</option><option value="pending">Pending</option></select><div class="flex justify-end gap-2"><button type="button" @click="open=false">Batal</button><button class="rounded-lg bg-brand-500 px-4 py-2 text-white">Simpan</button></div></form></div></div>@endsection
