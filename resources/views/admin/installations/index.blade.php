@extends('admin.layout',['title'=>'Perangkat']) @section('content')<section class="overflow-x-auto rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[.03]"><table class="w-full text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-5 py-3">Pengguna</th><th>Perangkat</th><th>Aktivasi</th><th>Terakhir online</th><th>Status</th><th></th></tr></thead><tbody>@forelse($installations as $installation)<tr class="border-t border-gray-100"><td class="px-5 py-4">{{ $installation->license->user->email }}</td><td class="max-w-xs truncate">{{ $installation->device_name ?: 'Tidak diketahui' }}</td><td>{{ $installation->activated_at?->format('d M Y H:i') }}</td><td>{{ $installation->last_online_at?->diffForHumans() ?: '-' }}</td><td>{{ $installation->revoked_at?'dicabut':'aktif' }}</td><td class="pr-5"><form method="POST" action="{{ route('admin.installations.update',$installation) }}">@csrf @method('PUT')<input type="hidden" name="active" value="{{ $installation->revoked_at?1:0 }}"><button class="{{ $installation->revoked_at?'text-success-600':'text-error-600' }}">{{ $installation->revoked_at?'Aktifkan':'Cabut' }}</button></form></td></tr>@empty<tr><td colspan="6" class="p-12 text-center">Belum ada perangkat.</td></tr>@endforelse</tbody></table></section><div class="mt-4">{{ $installations->links() }}</div>@endsection
